apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: docker-backend
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/docker-backend: "true"
  podSelector:
    matchLabels:
      io.kompose.network/docker-backend: "true"
